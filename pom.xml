<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.0</version>
        <relativePath/>
    </parent>

    <groupId>saas.identity</groupId>
    <artifactId>platform</artifactId>
    <version>0.1.0</version>
    <name>saas-identity-platform-springboot</name>
    <description>SaaS Identity Platform — Spring Boot backend consuming @saas/identity-platform-shared TypeSpec codegen.</description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!--
          /actuator/health 健康探针: Docker HEALTHCHECK 探针 + CORS 之外的
          唯一可用于"应用真的在服务 vs 端口开着"的语义信号。
          Probes (liveness/readiness) 在 application.yml 里手动开。
          spring-boot-starter-parent 锁定版本, 不显式声明。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- M09.Database — JPA + Flyway + Hibernate (ADR-0007) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations-jakarta</artifactId>
            <version>2.2.22</version>
        </dependency>

        <!--
          v0.1.13 起: Swagger UI 在线访问。
          springdoc-openapi-starter-webmvc-ui 自动从 swagger v3 注解 + Spring MVC
          端点收集 OpenAPI 3.1 doc, 暴露:
            - /v3/api-docs (OpenAPI JSON) 与 /v3/api-docs.yaml (YAML)
            - /swagger-ui.html (重定向到 /swagger-ui/index.html)
            - /swagger-ui/** (静态资源)
          SSOT 仍是 saas-identity-platform-shared/generated/openapi/openapi.yaml
          (openapi-generator 仓把 spec 拷生成本仓 controllers 接口, 注解直接合入
          runtime OpenAPI doc, 不需要维护两份)。
          版本与 Spring Boot 捆绑的 spring-web 强耦合: spring-web 6.2 (Boot 3.4) 删除了
          ControllerAdviceBean.<init>(Object), springdoc < 2.7.0 调它会 NoSuchMethodError
          (springdoc #2687, 回归测试 SpringdocCompatibilityTest)。2.8.x 支持 Boot 3.4/3.5。
        -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.9</version>
        </dependency>
        <dependency>
            <groupId>org.openapitools</groupId>
            <artifactId>jackson-databind-nullable</artifactId>
            <version>0.2.6</version>
        </dependency>

        <!--
          M09.Database — Hibernate 6 自定义 JDBC 类型：uuid[] / String[] / 等 PG 原生数组列。
          替代 @Convert(UuidArrayConverter) 那种 AttributeConverter 写法——后者声明返回 Java 数组，
          Hibernate 找不到对应的 JDBC SQL type code，启动期 buildStaticUpdateGroup 会崩。
          hypersistence-utils-hibernate-63 提供 UuidArrayType / StringArrayType UserType 实现，
          正确声明 Types.ARRAY + nullSafe Get/Set 走 JDBC Array API。
          Phase 6：testcontainers + saas_test 干净库跑 Flyway drift detection，本 dep 保留用于运行时。
        -->
        <dependency>
            <groupId>io.hypersistence</groupId>
            <artifactId>hypersistence-utils-hibernate-63</artifactId>
            <version>3.9.0</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-launcher</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <version>1.20.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>1.20.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>2.43.0</version>
                <configuration>
                    <java>
                        <googleJavaFormat/>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <id>spotless-check</id>
                        <phase>verify</phase>
                        <goals><goal>check</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>4.9.3.2</version>
                <configuration>
                    <excludeFilterFile>spotbugs-exclude.xml</excludeFilterFile>
                    <effort>Max</effort>
                    <threshold>Low</threshold>
                </configuration>
                <executions>
                    <execution>
                        <id>spotbugs-check</id>
                        <phase>verify</phase>
                        <goals><goal>check</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>